[user]
    name = Colin Campbell
    email = colin@elusivelabs.com
[core]
    editor = vim
[color]
    ui = true
[alias]
    co = checkout
    st = status
    ci = commit
    br = branch
    pr = pull --rebase
    ll = log --oneline --abbrev-commit --all --graph --decorate
    up = "!git remote update -p; git merge --ff-only @{u}"

[pull]
    rebase = true
